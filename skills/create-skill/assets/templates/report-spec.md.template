# 报告格式

1. **结论**：{{一句话结论，例如 可交付 / 不可交付（缺 N 项）}}。
2. **输入**：{{用户给的参数回显}}。
3. **指标**：{{指标名称与数值}}。
4. **函数结果**：{{关键字段；列表类结果只展示前 N 项，写明「展示前 N 项，共 X 项」}}。
5. **行动**：未执行 / 已执行（回执要点：{{字段}}）。
6. **依据**：本次调用的指标、函数、行动名称，供用户在 Trace 中核对。
