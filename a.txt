SECTIONS
  GROUPS    nested command groups — one level deeper
  READ      changes nothing
  RUN       acts without changing configuration (triggers a job, spends a model call, rotates a token)
  WRITE     changes platform state — confirm with a person first
  COMMANDS  not sorted into a section yet

auth                           SIGN IN & SETTINGS  Log in; the token is saved and reused. Start here.
  login                        WRITE               Log in to a platform (attach a token, or browser/password OAuth)
  status                       READ                Show base URL and whether a token is configured
  token                        RUN                 Print the current access token, refreshing it if expired (keep secret)
  whoami                       READ                Show current user identity (from the token)
  list                         READ                List saved sessions (platform → users; * = active)
  use                          WRITE               Switch the active platform
  logout                       WRITE               Remove the stored token for the active platform
  delete                       WRITE               Delete saved credentials for a platform
  switch                       WRITE               Switch the active user for a platform (by username or user id)
  users                        READ                List saved users for a platform (* = active)
  export                       RUN                 Export the active session's tokens (for a headless host)
  change-password              WRITE               Change your account password (bkn-safe self-service; no browser)
config                         SIGN IN & SETTINGS  Remember a platform URL / business domain
  show                         READ                Show the active platform and business domain
  set                          WRITE               Set a config value (baseUrl | businessDomain)
  set-bd                       WRITE               Set the default business domain for the active platform
  list-bd                      READ                List business domains (requires login)
appkey                         SIGN IN & SETTINGS  Issue long-lived `bak_` keys for scripts and services
  list                         READ                List your own AppKeys, no secrets → {keys}
  create                       WRITE               Issue an AppKey — the plaintext key is shown ONCE, on create
  regenerate                   WRITE               Rotate a key in place — mints a new plaintext; the old bak_ dies immediately
  revoke                       WRITE               Revoke one of your AppKeys (immediate; use the `id`, not `key_id`)
  admin                        GROUPS              Admin governance over all AppKeys
    list                       READ                List all AppKeys, or one owner's (adds owner_user_id)
    revoke                     WRITE               Revoke any AppKey by id
bkn                            DATA & KNOWLEDGE    Knowledge networks: schema, metrics, search, import/export
  list                         READ                List knowledge networks
  get                          READ                Get a knowledge network (use --stats or --export)
  search                       READ                Recall instances from a plain sentence — no object type or field names needed → {nodes, object_types}
  object-type                  GROUPS              object-type list/get/...
    list                       READ                List object-types
    get                        READ                Get object-type
    create                     WRITE               Create object-type (--body / --body-file)
    update                     WRITE               Update object-type (--body / --body-file)
    delete                     WRITE               Delete object-type
    query                      RUN                 Query instances of an object type (--body / --body-file JSON)
  relation-type                GROUPS              relation-type list/get/...
    list                       READ                List relation-types
    get                        READ                Get relation-type
    create                     WRITE               Create relation-type (--body / --body-file)
    update                     WRITE               Update relation-type (--body / --body-file)
    delete                     WRITE               Delete relation-type
  action-type                  GROUPS              action-type list/get/...
    list                       READ                List action-types
    query                      RUN                 Query an action type (--body / --body-file JSON)
    execute                    RUN                 Execute an action type (--body / --body-file envelope JSON)
    get                        READ                Get an action type
  stats                        READ                Get knowledge-network statistics (alias for get --stats)
  export                       READ                Export a knowledge network (alias for get --export)
  create                       WRITE               Create an (empty) knowledge network
  update                       WRITE               Update a knowledge network (--body / --body-file)
  delete                       WRITE               Delete a knowledge network
  subgraph                     READ                Query a subgraph (--body / --body-file JSON)
  action-log                   GROUPS              Action logs — list/get/cancel; list pages with search_after
    list                       READ                List action logs
    get                        READ                Get an action log
    cancel                     RUN                 Cancel a running action
  action-execution             READ                Get action execution status
  metric                       GROUPS              Metrics — query / dry-run
    query                      RUN                 Query a metric's data (--body / --body-file JSON)
    dry-run                    RUN                 Dry-run a metric definition (--body / --body-file JSON)
    list                       READ                List metrics
    get                        READ                Get a metric
    create                     WRITE               Create a metric (--body / --body-file)
    update                     WRITE               Update a metric (--body / --body-file)
    delete                     WRITE               Delete a metric
    validate                   RUN                 Validate a metric definition (--body / --body-file)
  concept-group                GROUPS              Concept groups — list/get
    list                       READ                List concept groups
    get                        READ                Get a concept group
    create                     WRITE               Create a concept group (--body / --body-file)
    update                     WRITE               Update a concept group (--body / --body-file)
    delete                     WRITE               Delete a concept group
    add-members                WRITE               Add object types to a concept group (--body / --body-file)
    remove-members             WRITE               Remove object types (comma-joined ids) from a concept group
  action-schedule              GROUPS              Action schedules — list/get
    list                       READ                List action schedules
    get                        READ                Get an action schedule
    create                     WRITE               Create an action schedule (--body / --body-file)
    update                     WRITE               Update an action schedule (--body / --body-file)
    set-status                 WRITE               Set an action schedule's status (--body / --body-file)
    delete                     WRITE               Delete action schedule(s) (comma-joined ids)
  push                         WRITE               Pack a BKN directory into a tar and import it as a knowledge network
  pull                         READ                Download a knowledge network as a BKN tar and extract it locally
  relation-type-paths          READ                Query relation-type paths between object types (--body / --body-file JSON)
  resources                    READ                List BKN-backend resources
  create-from-catalog          WRITE               Build a knowledge network from a Vega catalog's tables
  validate                     READ                Validate a local BKN directory's structure (offline)
vega                           DATA & KNOWLEDGE    Data sources: catalogs, connectors, SQL, index builds
  catalog                      GROUPS              Catalog entries
    list                       READ                List catalog entries
    get                        READ                Get a catalog by id
    resources                  READ                List resources under a catalog
    health                     READ                Health-status for a catalog
    create                     WRITE               Create a catalog (data source)
    update                     WRITE               Fully update a catalog
    enable                     WRITE               Enable a catalog (required before discovery)
    disable                    WRITE               Disable a catalog
    delete                     WRITE               Delete a catalog
    test-connection            RUN                 Test a catalog connection
    test-connection-config     RUN                 Test an unpersisted catalog connection configuration
    health-check-schedule      READ                Get a catalog health-check schedule
    set-health-check-schedule  WRITE               Update a catalog health-check schedule
    discover                   RUN                 Trigger catalog resource discovery
  discover-schedule            GROUPS              Resource discovery schedules
    list                       READ                List discovery schedules
    get                        READ                Get a discovery schedule
    create                     WRITE               Create a discovery schedule
    update                     WRITE               Fully update a discovery schedule
    enable                     WRITE               Enable a discovery schedule
    disable                    WRITE               Disable a discovery schedule
    delete                     WRITE               Delete a discovery schedule
  discover-task                GROUPS              Resource discovery tasks
    list                       READ                List discovery tasks
    get                        READ                Get a discovery task
    delete                     WRITE               Delete completed discovery tasks
  semantic-task                GROUPS              Semantic-understanding tasks
    list                       READ                List semantic-understanding tasks
    create                     WRITE               Create a semantic-understanding task
    get                        READ                Get a semantic-understanding task
    delete                     WRITE               Delete completed semantic-understanding tasks
  connector-type               GROUPS              Connector types
    list                       READ                List connector types
    get                        READ                Get a connector type
  sql                          RUN                 Read-only SQL straight against a data source — no knowledge network, no Trace record
  resource                     GROUPS              Vega-backend resources
    list                       READ                List resources
    get                        READ                Get a resource
    discover                   RUN                 Trigger metadata discovery for a resource
    enable                     WRITE               Enable a resource
    disable                    WRITE               Disable a resource
    query                      RUN                 Fetch data rows from a resource
    document-get               READ                Get one dataset document
    document-create            WRITE               Create dataset documents
    document-upsert            WRITE               Upsert dataset documents; every document must have an id
    document-delete            WRITE               Delete dataset documents by id
    document-delete-filter     WRITE               Delete dataset documents by a non-empty filter
  dataset                      GROUPS              Dataset index build tasks
    build                      RUN                 Build a resource's index (creates a BuildTask)
    build-status               RUN                 Show a BuildTask's state and progress
    build-list                 RUN                 List BuildTasks
    build-start                WRITE               Start a BuildTask
    build-stop                 WRITE               Stop a BuildTask
    build-delete               WRITE               Delete one or more BuildTasks
resource                       DATA & KNOWLEDGE    Tables and views behind a network: find, inspect, sample, enable, disable
  list                         READ                List resources under a catalog
  find                         READ                Search resources by name, fuzzy unless --exact → a bare array, not an envelope
  get                          READ                Get resource details
  enable                       WRITE               Enable a resource
  disable                      WRITE               Disable a resource
  query                        RUN                 Fetch data rows from a resource
  delete                       WRITE               Delete a resource
context                        DATA & KNOWLEDGE    Ask a network questions (the MCP interface agents use)
  search-schema                READ                Search object/relation/action/metric schemas → {object_types, relation_types, action_types, metric_types}
  query-object-instance        RUN                 Query one object type's instances — `--args '{"ot_id":"<id>","limit":10}'` → {datas, total_count}
  find-skills                  READ                Recall skills for an object type
  kn-detail                    READ                Get a KN's schema at a detail level (progressive: summary skeleton → drill down)
  object-types                 READ                Full definitions for the given object-type ids (unmatched → `missing`)
  relation-types               READ                Full definitions for the given relation-type ids (unmatched → `missing`)
  conversation                 GROUPS              Show the remembered conversation, or forget it with --forget
  info                         READ                List the deploy's MCP tool catalog (global — no KN needed)
  run-sql                      RUN                 Aggregate, rank or join with read-only SQL — what query-object-instance cannot do
  explore-subgraph             RUN                 Follow relations outward from one object type without naming a path first
  query-metric                 RUN                 Read a modelled metric through its own definition — do not restate it in SQL
  tools                        READ                List MCP tools advertised for a KN session → {tools} with each inputSchema
  tool-call                    RUN                 Call any MCP tool by name — current or future (use `tools` to discover)
  call-method                  RUN                 Call any MCP method by name (e.g. tools/list, resources/read) — current or future
  resources                    READ                List MCP resources
  resource                     READ                Read one MCP resource by uri
  templates                    READ                List MCP resource templates
  prompts                      READ                List MCP prompts
  prompt                       READ                Get one MCP prompt (--args JSON for prompt arguments)
  query-instance-subgraph      RUN                 Query an instance subgraph across relation-type paths
  get-logic-properties         RUN                 Compute logic-property values for instances
  get-action-info              RUN                 Fetch action info / dynamic tools for an instance
model                          MODELS              Large and small models: chat, embeddings, rerank, defaults
  llm                          GROUPS              Large language models
    list                       READ                List LLM models → {data, count}
    get                        READ                Get an LLM model
    chat                       RUN                 OpenAI-compatible chat completion (<model> = model name or numeric id)
    set-default                WRITE               Set this LLM as the system default (admin)
    unset-default              WRITE               Clear this LLM as the system default (admin)
    add                        WRITE               Register a model (definition JSON via --body / --body-file)
    edit                       WRITE               Update a model definition (JSON via --body / --body-file)
    delete                     WRITE               Delete model(s) (comma-joined ids)
    test                       RUN                 Test a model's connectivity / inference (JSON via --body / --body-file)
  small                        GROUPS              Small models (embedding / reranker)
    list                       READ                List small models → {data, count}
    get                        READ                Get a small model
    embeddings                 RUN                 Compute embeddings (<model> = model name or numeric id)
    rerank                     RUN                 Rerank documents against a query (<model> = model name or numeric id)
    get-default                READ                Show the system default small model for a type
    set-default                WRITE               Set this small model as the system default for its type (admin)
    unset-default              WRITE               Clear this small model as the system default (admin)
    add                        WRITE               Register a model (definition JSON via --body / --body-file)
    edit                       WRITE               Update a model definition (JSON via --body / --body-file)
    delete                     WRITE               Delete model(s) (comma-joined ids)
    test                       RUN                 Test a model's connectivity / inference (JSON via --body / --body-file)
skill                          TOOLS & SKILLS      Skill packages (SKILL.md + files) agents load on demand
  list                         READ                List skills → {data, total, page, page_size, has_next}; the id to reuse is `skill_id`
  get                          READ                Get a skill by id
  market                       READ                Browse the skill market → {data, total, page, has_next}
  market-get                   READ                Get a market skill by id
  delete                       WRITE               Delete a skill
  content                      READ                Read a skill's SKILL.md content index
  read-file                    READ                Read a file inside a skill (progressive)
  files                        READ                List a skill's files (one level; --tree for the whole hierarchy)
  names                        READ                Resolve skill ids to names (unknown ids are skipped)
  execute                      RUN                 Run a skill in the platform sandbox
  history                      READ                Show a skill's version history
  set-status                   WRITE               Change status: unpublish | published | offline
  register                     WRITE               Zip a local skill directory and register it
  download                     RUN                 Download a skill archive to a local .zip
  install                      RUN                 Download a skill archive and extract it locally
  update-metadata              WRITE               Update a skill's metadata (--body / --body-file JSON)
  update-package               WRITE               Replace a skill's package from a local directory
  republish                    WRITE               Republish a previous skill version
  publish-history              WRITE               Publish a historical skill version
toolbox                        TOOLS & SKILLS      Toolboxes: group tools into one publishable box
  list                         READ                List toolboxes
  create                       WRITE               Create a toolbox
  publish                      WRITE               Publish a toolbox
  unpublish                    WRITE               Unpublish a toolbox (status=draft)
  delete                       WRITE               Delete a toolbox
  export                       READ                Export a toolbox config to a local .adp file
  import                       WRITE               Import a toolbox config from a local .adp file
tool                           TOOLS & SKILLS      Tools in a box: upload an OpenAPI spec, enable, call
  list                         READ                List tools in a toolbox
  enable                       WRITE               Enable one or more tools
  disable                      WRITE               Disable one or more tools
  execute                      RUN                 Invoke a published+enabled tool
  debug                        RUN                 Invoke a tool (draft/disabled too)
  upload                       WRITE               Upload a tool definition file (OpenAPI spec) into a toolbox
trace                          TRACING             Inspect what an agent actually did, and diagnose bad answers
  graph                        READ                Fetch normalized trace graph by trace id
  conversations                GROUPS              Manage Trace conversations
    list                       READ                List conversations in the authorized owner scope
    ensure-current             RUN                 Ensure the current Core-owned conversation generation
    create-new-generation      RUN                 Create the next Core-owned conversation generation
    resume                     RUN                 Resume an existing authorized conversation
    get                        READ                Get an authorized conversation
    close                      RUN                 Close an active conversation
  interactions                 GROUPS              Inspect Trace interactions
    start                      RUN                 Start one managed interaction
    get                        READ                Get an authorized interaction
    operations                 RUN                 List the exact Operation call facts for one interaction
    complete                   RUN                 complete a managed interaction using a 3.0 completion manifest
    fail                       RUN                 fail a managed interaction using a 3.0 completion manifest
    cancel                     RUN                 cancel a managed interaction using a 3.0 completion manifest
    handoff                    RUN                 handoff a managed interaction using a 3.0 completion manifest
  operations                   GROUPS              Inspect and retry operations
    get                        READ                Get an authorized operation
    attempt                    RUN                 Get one exact Operation attempt call fact
    retry                      RUN                 Create the next retry attempt for an eligible failed operation
  receipts                     GROUPS              Inspect durable operation receipts
    get                        READ                Get an authorized operation receipt
  get                          READ                Fetch normalized spans for a conversation
  detail                       READ                Get one typed technical trace with Span and Operation facts
  spans                        READ                Fetch normalized spans for a conversation
  search                       READ                List authorized technical traces → {entries, total, next_cursor, partial}
  diagnose                     RUN                 Diagnose a conversation's trace (symbolic rules; --llm adds rubric judging)
  scan                         RUN                 Batch-diagnose several conversations (comma-joined) + aggregate findings
  eval-set                     GROUPS              Build trace eval sets
    build                      RUN                 Build eval cases from a queries JSON file
  schema                       GROUPS              Validate eval-set / diagnosis-rule files
    validate                   RUN                 Validate an eval-set or diagnosis-rule file (JSON/YAML) against its schema
  validate-fixture             RUN                 Validate BKN Trace 1.0/2.0/2.1 fixture JSON files
admin                          ADMINISTRATION      Orgs, users, roles, license, audit log
  auth                         GROUPS              Operator authentication
    login                      WRITE               Log in to a platform (attach a token, or browser/password OAuth)
    status                     READ                Show base URL and whether a token is configured
    token                      RUN                 Print the current access token, refreshing it if expired (keep secret)
    whoami                     READ                Show current user identity (from the token)
    list                       READ                List saved sessions (platform → users; * = active)
    use                        WRITE               Switch the active platform
    logout                     WRITE               Remove the stored token for the active platform
    delete                     WRITE               Delete saved credentials for a platform
    switch                     WRITE               Switch the active user for a platform (by username or user id)
    users                      READ                List saved users for a platform (* = active)
    export                     READ                Export the active session's tokens (for a headless host)
    change-password            WRITE               Change your account password (bkn-safe self-service; no browser)
  org                          GROUPS              Departments and org structure
    list                       READ                List departments → {departments, total}
    get                        READ                Get one department
    members                    READ                List members of a department
    create                     WRITE               Create a department
    update                     WRITE               Update a department (only provided fields change)
    delete                     WRITE               Delete a department
    tree                       READ                Print the department hierarchy
  user                         GROUPS              User management
    list                       READ                List users → {users, total}
    assign-role                WRITE               Grant a role to a user
    revoke-role                WRITE               Revoke a role from a user
    get                        READ                Get one user
    roles                      READ                List roles granted to a user
    create                     WRITE               Create a user (gets the platform default password)
    update                     WRITE               Update a user (only provided fields change)
    delete                     WRITE               Delete a user
    reset-password             WRITE               Reset a user's password (defaults to the platform initial password)
  role                         GROUPS              Role management
    list                       READ                List roles → {roles}
    get                        READ                Get a role by id
    members                    READ                List members of a role
    add-member                 WRITE               Add a member to a role
    remove-member              WRITE               Remove a member from a role
    create                     WRITE               Create a custom role (bkn-safe; built-in roles are read-only)
    update                     WRITE               Update a custom role's name/description (403 on built-in)
    delete                     WRITE               Delete a custom role (403 on built-in)
    grant-perm                 WRITE               Grant a permission on a custom role (403 on built-in)
    revoke-perm                WRITE               Revoke a permission on a custom role (403 on built-in)
  llm                          GROUPS              llm management
    list                       READ                List llm models
    get                        READ                Get a llm model
    add                        WRITE               Register a llm model (granular flags or --body/--body-file)
    edit                       WRITE               Edit a llm model (granular flags or --body/--body-file)
    delete                     WRITE               Delete llm model(s)
    test                       RUN                 Test a llm model
  small-model                  GROUPS              small-model management
    list                       READ                List small-model models
    get                        READ                Get a small-model model
    add                        WRITE               Register a small-model model (granular flags or --body/--body-file)
    edit                       WRITE               Edit a small-model model (granular flags or --body/--body-file)
    delete                     WRITE               Delete small-model model(s)
    test                       RUN                 Test a small-model model
  license                      GROUPS              Cluster license management
    show                       READ                Current license detail (state/edition/expiry/features)
    import                     WRITE               Import a .lic license file (online deployments auto-activate)
    receipt                    RUN                 Import an offline activation receipt (.lic from the license portal)
    activate                   WRITE               Report the installed license to the issuer (online deployments)
    remove                     WRITE               Remove the installed license (back to unactivated)
    fingerprint                RUN                 This cluster's machine code (paste into the license portal for offline activation)
  audit                        GROUPS              Audit log queries
    list                       READ                List login audit events
  config                       GROUPS              Admin CLI config (active platform)
    show                       READ                Show the active platform
    set                        WRITE               Set a config value (baseUrl)
  call                         RUN                 Operator API passthrough (curl-style; auto-injected auth) — paths at https://openbkn-ai.github.io/bkn-foundry/
call                           RAW API             Call any platform API endpoint directly (auth added)
