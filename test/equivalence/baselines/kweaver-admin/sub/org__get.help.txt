Usage: kweaver-admin org get [options] <id>

Show full JSON for one department

Arguments:
  id          Department id (if the id starts with `-`, pass it after `--`, e.g.
              `org get -- -1`)

Options:
  -h, --help  display help for command
